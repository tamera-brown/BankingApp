#Bank

##Compile the code
```
./gradlew clean build
```

##Build Docker image
```
docker-compose build
```

##Run the app
```
docker-compose up
```

##Swagger UI
```
http://localhost:8080/swagger-ui/index.html
```